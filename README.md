OCR Microservice for Medical Reports
A containerized FastAPI backend designed to extract text from noisy, low-quality medical document scans. It implements a multi-stage computer vision pipeline to handle common OCR challenges like shadows, table grids, and mixed font weights.
